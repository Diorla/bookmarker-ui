import { keyframes } from 'styled-components';

export default keyframes`
  from {
    width: 1.25rem;
    height: 1.25rem;
    opacity: 1;
  }
  to {
    width: 10rem;
    height: 10rem;
    opacity: 0.5;
  }
`;
