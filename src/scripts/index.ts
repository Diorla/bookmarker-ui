import attachEvent from './attachEvent';
import { lighten, darken, contrastColor } from './color-functions';
import detachEvent from './detachEvent';

export { lighten, darken, attachEvent, detachEvent, contrastColor };
